<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!--
        Intentional version pinning. These are informational checks that only
        suggest availability of newer versions. Upgrading AGP 8.x -> 9.x,
        targetSdk 36 -> 37, or bumping androidx minors is a separate,
        risk-controlled task and must not happen in this hardening commit.
    -->
    <issue id="OldTargetApi" severity="ignore" />
    <issue id="AndroidGradlePluginVersion" severity="ignore" />
    <issue id="GradleDependency" severity="ignore" />
</lint>